"use client";

import React from "react";
import HeroEditorial from "./HeroEditorial";

// One hero, fully art-directed. The old variant switcher is retired — the
// alternate heroes still live in this folder if we ever want them back.
export default function Hero() {
  return <HeroEditorial />;
}
